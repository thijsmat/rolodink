name: Publish Firefox Extension

on:
  release:
    types: [published]

jobs:
  publish-firefox:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Download Firefox Artifact
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release download ${{ github.event.release.tag_name }} -p "Rolodink-firefox-*.zip"
          echo "ZIP_FILE=$(ls Rolodink-firefox-*.zip)" >> $GITHUB_ENV

      - name: Install web-ext
        run: npm install -g web-ext

      - name: Publish to Mozilla Add-ons
        env:
          WEB_EXT_API_KEY: ${{ secrets.FIREFOX_JWT_ISSUER }}
          WEB_EXT_API_SECRET: ${{ secrets.FIREFOX_JWT_SECRET }}
        run: |
          web-ext sign \
            --channel listed \
            --source-dir ${{ env.ZIP_FILE }} \
            --api-key $WEB_EXT_API_KEY \
            --api-secret $WEB_EXT_API_SECRET
