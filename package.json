{
  "name": "linkedin-crm-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "workspaces": [
    "linkedin-crm-backend",
    "linkedin-crm-extension",
    "website"
  ],
  "devDependencies": {
    "eslint": "^8",
    "turbo": "^2.6.0",
    "typescript": "^5"
  },
  "packageManager": "npm@10.8.1",
  "dependencies": {
    "glob": "10.5.0"
  }
}
