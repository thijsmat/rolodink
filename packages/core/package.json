{
  "name": "@rolodink/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Platform-agnostic Rolodink domain logic: crypto, URL normalization, API client.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf node_modules/.vite"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^2.1.9"
  }
}
