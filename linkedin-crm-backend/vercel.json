{
  "version": 2,
  "framework": "nextjs",
  "ignoreCommand": "bash -lc 'if git diff --name-only $VERCEL_GIT_PREVIOUS_SHA $VERCEL_GIT_COMMIT_SHA | grep -q \"^linkedin-crm-backend/\"; then exit 1; else echo \"No backend changes, ignoring deploy\"; exit 0; fi'"
}


